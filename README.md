# CoNuVar

This repo is for **Co**py **Nu**mber **Va**riation, a.k.a. CoNuVar.

Zeyu Li

## TODO
- [x] Change initialization
- [x] Evaluate cost by following equation, $$\sum_i(y'_i - y_i)$$
- [x] Change sampling weight from w.r.t `count` to w.r.t `TPM` from dataset.
- [x] Output `ground truth`, `FoldChange`, and `predicted y` to a file.
